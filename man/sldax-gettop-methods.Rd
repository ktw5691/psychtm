% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slda-generic-functions.R, R/slda-results.R
\name{sldax-gettop-methods}
\alias{sldax-gettop-methods}
\alias{get_toptopics}
\alias{get_topwords}
\alias{get_zbar}
\alias{gg_coef}
\alias{est_theta}
\alias{est_beta}
\alias{get_toptopics,matrix-method}
\alias{get_topwords,matrix,numeric,character-method}
\alias{get_zbar,Sldax-method}
\alias{gg_coef,Sldax-method}
\alias{est_theta,Sldax-method}
\alias{est_beta,Sldax-method}
\title{Compute and visualize the most probable topics in a document or
the most probable words in a topic.}
\usage{
get_toptopics(theta, ntopics)

get_topwords(beta_, nwords, vocab, method = "termscore")

get_zbar(mcmc_fit, burn = 0, thin = 1)

gg_coef(mcmc_fit, burn = 0, thin = 1, stat = "mean", errorbw = 0.5)

est_theta(mcmc_fit, burn = 0, thin = 1, stat = "mean")

est_beta(mcmc_fit, docs, burn = 0, thin = 1, stat = "mean")

\S4method{get_toptopics}{matrix}(theta, ntopics)

\S4method{get_topwords}{matrix,numeric,character}(beta_, nwords, vocab, method = "termscore")

\S4method{get_zbar}{Sldax}(mcmc_fit, burn = 0, thin = 1)

\S4method{gg_coef}{Sldax}(mcmc_fit, burn = 0, thin = 1, stat = "mean", errorbw = 0.5)

\S4method{est_theta}{Sldax}(mcmc_fit, burn = 0, thin = 1, stat = "mean")

\S4method{est_beta}{Sldax}(mcmc_fit, docs, burn = 0, thin = 1, stat = "mean")
}
\arguments{
\item{theta}{A D x K matrix of K topic proportions for all D documents.}

\item{ntopics}{The number of topics to retrieve (default: all topics).}

\item{beta_}{A \eqn{K} x \eqn{V} matrix of word-topic probabilities; each row
sums to 1.}

\item{nwords}{The number of words to retrieve (default: V).}

\item{vocab}{A character vector of length V containing the vocabulary.}

\item{method}{If "termscore", use term scores (similar to tf-idf). If "prob",
use probabilities (default: "termscore").}

\item{mcmc_fit}{An object of class \code{\link{Sldax}}.}

\item{burn}{The number of draws to discard as a burn-in period (default: 0).}

\item{thin}{The number of draws to skip as a thinning period (default: 1; i.e., no thinning).}

\item{stat}{The summary statistic to use on the posterior draws (default: \code{"mean"}).}

\item{errorbw}{Positive control parameter for the width of the +/- 2
posterior standard error bars (default: 0.5).}

\item{docs}{A D x max(N_d) matrix of the words in all documents. Unused word
positions should be set to 0.}
}
\value{
A \eqn{K} x \eqn{V} matrix of term-scores (comparable to tf-idf).
}
\description{
\code{get_toptopics()} creates a \code{tibble} of topics and the top
\code{nwords} words per topic sorted by probability or term score.
\code{get_zbar()} computes empirical topic proportions from slot
\code{@topics} in an object of class \code{\link{Sldax}}.
\code{gg_coef()} plots regression coefficients for \code{\link{Sldax}} models.
\code{est_theta()} estimates the mean or median theta matrix.
\code{est_beta()} estimates the mean or median beta matrix.
}
